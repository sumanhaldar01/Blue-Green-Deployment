events {}

http {
    upstream app_backend {
        server blue:3000;  # default active
        # server green:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://app_backend;
        }
    }
}